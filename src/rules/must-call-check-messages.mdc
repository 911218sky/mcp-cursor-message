---
alwaysApply: true
---
當使用者正在使用側邊欄佇列，且已載入 `mcp-cursor-message` MCP 伺服器時，助理應遵守下列規範。

## 佇列收尾：`check_messages`

- 在**每一次**助理回覆中，於結束對使用者的訊息之前，**最後一個 MCP 工具呼叫**必須是 `mcp-cursor-message` 伺服器上的 **`check_messages`**。
- 可先呼叫其他 MCP 工具（例如 `ask_question`、`send_progress`），但結束本輪前 **`check_messages` 仍須為最後一次**。
- 若可行，請透過 `reply` 參數傳入**本輪完整回覆**（支援 Markdown），讓擴充功能能顯示並銜接下一則佇列訊息。
- 若 `check_messages` 回傳「沒有新訊息／等待已被中斷／請再次呼叫」等**系統提示**，請**不要**把該段原文當成給使用者的答案；應在**同一次回覆流程內**重複呼叫 `check_messages`，直到取得實際佇列內容（或工具被強制終止）。

## 長步驟與進度：`send_progress`

- 任務會分成多步（例如分析 → 實作 → 驗證）時，建議在**每個關鍵步驟完成後**呼叫 **`send_progress`**，以簡短 Markdown 說明已完成事項、目前狀態與下一步。

## 首次協作（onboarding）

- 與使用者在側欄佇列的**第一次有效協作**，或使用者**尚未**在側欄完成「協作偏好」設定時：應盡快透過 **`ask_question`** 完成 onboarding（**一則呼叫可含多題**）。
- **必備（至少問過）**：
  - **提問／確認頻率**：每次先問／不確定才問／由 AI 自行決定何時問；
  - **效率取向**：偏快（少停頓、精簡步驟）／平衡／偏穩（多檢查、多說明與驗證）；
  - **subagent 使用程度**：盡量自行處理／適度（較大或探索型任務再用）／積極（並行探索、長任務優先委派）。
- **擴充（建議同首輪或下一輪補齊，仍只用 `ask_question`）**：利於約定「之後怎麼處理」，可依專案再增刪題目：
  - **使用者可見文件**：行為或契約變更時，是否一併更新 `README.md`／`PROJECT.md`（或「僅在要求時」）；
  - **Git／提交**：在遵守儲存庫規範（例如未同意不得 `commit`／`push`）的前提下，助理是否**僅提供建議指令**、或當你**明文授權當次**才可代為執行 `git commit`；
  - **失敗與卡住**：工具或指令失敗時，**先自主重試與替代做法**再側欄求助，或**較早**用 `ask_question` 請你決策；
  - **回覆語言**：例：繁中為主、英文為主、或技術段落英文其餘中文；
  - **其他約定**（可自訂選項）：例如寧可偏長說明／寧可精簡、是否預設跑 `compile`／`package`、機敏步驟邊界等。
- 取得側欄答案後，後續回合依約執行；使用者若要改設定，應再以 **`ask_question`** 重設，勿只在聊天口頭約定。

## 釐清需求與選項：`ask_question`

- **凡需要使用者回答、選擇、確認或補充資料的提問，一律透過 `ask_question` 在側欄呈現**；**勿**僅在一般聊天訊息裡向使用者發問（說明與脈絡可寫在 `send_progress` 或 `check_messages` 的 `reply` 等非阻塞內容，但**期待對方回覆的問題本身**須走 `ask_question`）。若某情境確實無法用側欄表達，應**極力避免**；若別無他法，才可於聊天簡述原因並以最簡短方式處理。
- 需要確認需求、策略或取捨時，呼叫 **`ask_question`**，並提供 **2–4 個具體選項**（或工具所支援之自訂輸入方式），由使用者決定，避免助理自行假設。
- **個別任務是否使用 subagent**：除 onboarding 已約定的預設外，若該次任務與預設有衝突或屬高成本操作，仍應以 **`ask_question`** 讓使用者當次確認。

## 計畫模式（plan）：設計與複雜實作

- 當任務屬於**大型、含糊、或多種架構／實作路線並存**時，應優先考量以 Cursor **「計畫模式（plan）」**先收斂方案再動手。
- **特別地**：只要使用者提出的是**設計取向的需求**（例如 UI/UX 流程、互動規格、資料模型、功能範圍取捨、可擴充架構等），應透過 **`ask_question`** 詢問是否要先開啟「計畫模式」來設計實作方案（題干可含**一句**為何適合 plan 的理由）；**不要**在未經側欄選擇前就直接假定可邊做邊改。
- **僅在使用者於 `ask_question` 中同意切換後**，才呼叫 `SwitchMode` 將模式設為 `plan`。選項範例：**同意，切換到計畫模式**／**不要，維持目前模式**。
