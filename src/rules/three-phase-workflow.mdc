---
description: "AI 助手預設三階段工作流：先分析、再定案、最後實作"
alwaysApply: true
---
# AI 助手核心規則：三階段工作流

收到使用者訊息時，**預設從階段一開始**，除非使用者明確指定要從某一階段名稱起算。

若已載入 `mcp-cursor-message` 且依賴側欄佇列：**凡預期對方回答的提問**須遵守 `must-call-check-messages.mdc`（優先使用 **`ask_question`**，勿只在聊天裡空問）。

## 階段一：分析問題

**聲明格式**：`【分析問題】`

**目的**：可能有多種作法；要做出正確決策需要足夠依據。

**必須做的事**：

- 理解使用者意圖；若有歧義須提問釐清（佇列／側欄模式下用 **`ask_question`**）。
- 搜尋、閱讀所有相關程式與設定。
- 辨識根因與影響範圍。

**主動發現**（在不改碼前提下盡量觀察）：

- 重複邏輯、命名不佳、可刪除的死碼
- 可能過時或過度複雜的設計、呼叫鏈
- 型別或慣例不一致；並視情況擴大搜尋是否為通案

完成以上後，**向使用者提問**（有需要時）：若有多種無法自決的方案，應列入提問內容。**若無需再問**，可直接進入階段二。

**絕對禁止**：

- ❌ 修改任何程式或專案檔案
- ❌ 急著給實作或跳過搜尋／理解
- ❌ 不分析就推薦方案

**階段轉換**：本階段以「提問／或確認可進入下一階段」收尾；無需提問則可進入階段二。

## 階段二：制定方案

**聲明格式**：`【制定方案】`

**前置條件**：使用者已答覆關鍵技術決策（或本輪本來就無歧義且你在階段一已確認可依既有慣例執行）。

**必須做的事**：

- 列出將**新增、修改、刪除**的檔案，並簡述各檔變更重點。
- 若發現重複邏輯，須在方案中透過**復用或抽象**消弭，並符合 DRY 與合理架構。
- 若此時仍有意釐清點，可**繼續提問**（佇列模式下用 **`ask_question`**）；直到沒有未決事項，本階段結束。

**注意**：本階段**不得**自動切入階段三；須清楚交代方案並在你判定無未決項後，再進入階段三。

## 階段三：執行方案

**聲明格式**：`【執行方案】`

**必須做的事**：

- 依既定方案實作；變更後執行專案慣例的**型別／建置檢查**（本儲存庫為 `bun run compile`；若動到打包則依 `AGENTS.md`／`code-review-agents.mdc` 補跑 `bun run package`）。

**絕對禁止**：

- ❌ **逕自** `git commit`、`git push` 或等同上傳遠端；即使用者口頭說要提交，仍須依 `must-call-check-messages.mdc` 之 **Git** 小節以側欄 **`ask_question`** 確認範圍與訊息後再執行（與 `AGENTS.md` 併同遵守）。
- ❌ 擅自啟動開發伺服器（除非使用者明確要求且符合工作區規範）。

若在實作中發現無法自決的問題，**退回提問**（佇列模式下用 **`ask_question`**），勿臆測硬做。
